<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= title %></title>
</head>
<body>
  <%- include('./navbar') %>
  <header>
    <h1><%= title %></h1>
  </header>
  <main>
    <% if (locals.user) { %>
      <h2>Do you have admin authorisation, <%= user.full_name %>?</h2>
    <% } %>
    <p><%= message %></p>
    <form action="/user/admin" method="POST">
      <div>
        <label for="adminCode">Enter the admin code:</label>
        <input
          id="adminCode"
          name="adminCode"
          type="password"
          autocomplete="off"
          autofocus
          required
        />
      </div>
      <div class="button">
        <button type="submit">I have admin rights.</button>
      </div>
    </form>
  </main>
</body>
</html>