<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= title %></title>
</head>
<body>
  <%- include("./navbar") %>
  <header>
    <h1><%= title %></h1>
  </header>
  <main>
    <form action="/log-in" method="POST">
      <div>
        <label for="username">Username *</label>
        <input
          id="username"
          name="username"
          placeholder="example@email.com"
          type="email"
          autocomplete="off"
          minlength="3"
          maxlength="60"
          title="Email must be between 3 and 60 characters."
          required
          autofocus
        />
      </div>
      <div>
        <label for="password">Password *</label>
        <input
          id="password"
          name="password"
          type="password"
          autocomplete="off"
          pattern="^(?=.*\d])(?=.*[a-z])(?=.*[A-Z])(?=.*\W)(?!.* ).{8,24}$"
          title="Password must contain one number, one lowercase letter, one uppercase letter, one special character, no spaces, and be more than 8 characters long."
          required
        />
      </div>
      <div class="button">
        <button type="submit">Log in</button>
      </div>
    </form>
  </main>
</body>
</html>