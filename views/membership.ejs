<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= title %></title>
</head>
<body>
  <%- include("./navbar") %>
  <header>
    <h1><%= title %></h1>
  </header>
  <main>
    <% if (locals.user) { %>
      <h2>Would you like to become a member, <%= user.full_name %>?</h2>
    <% } %>
    <p><%= message %></p>
    <form action="/user/membership" method="POST">
      <div>
        <label for="passcode">Enter the secret passcode:</label>
        <input
          id="passcode"
          name="passcode"
          placeholder="LUCKYYOU"
          type="password"
          autocomplete="off"
          autofocus
          required
        />
      </div>
      <div class="button">
        <button type="submit">Make me a member!</button>
      </div>
    </form>
  </main>
</body>
</html>